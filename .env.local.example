# Server-side target for Next.js API rewrites (not exposed to the browser).
# Default backend: https://wastewatcher.onrender.com
# Set this to http://127.0.0.1:8000 when running FastAPI locally.
WASTEWATCHERS_API_ORIGIN=https://wastewatcher.onrender.com
